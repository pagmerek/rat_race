import { Request, Response, Router } from 'express';
import { Controller } from '../interfaces/Controller';


const getHomesite = (req: Request, res: Response): void => {

}

export default getHomesite;